"""Shared library for the design-craft pack.

Stdlib only, and on the gate path — so it may never gain a dependency. Copied into
each skill directory by the export step (M4), which is why nothing here may
reference a path outside its own package.

Contracts for the modules that will live here are frozen in lib/README.md and
Appendix A of docs/EXECUTION-PLAN.md; both are (proposed) until the modules land.
"""
